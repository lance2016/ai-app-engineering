"""A deliberately tiny MCP-shaped server and client over stdio.

Not the official SDK. It exists so lesson 11 can show the wire protocol
(JSON-RPC 2.0, newline-delimited) without hiding it behind decorators.
"""
