# AGENTS.md｜给 AI 协作者的续写说明

读者是 AI 编码助手（Claude Code、Codex、Cursor 等）。人类维护者会说「改一改第 N 课」，你要靠这份文件独立完成。

这门课只有一个标准：**给读者一份易读、正确的知识。** 每课按自己的主题组织，不套模板，不写 AI 腔。
下面所有规则都是这句话的展开。

## 1. 项目是什么

一门中文开源课程，**只讲理论和机制**，不提供可运行的项目代码。

| 层 | 目录 | 形式 |
|---|---|---|
| 背景知识 | `prerequisites/` | 两类：工程能力（`engineering-foundations.md`）和算法基础（`algorithm-foundations.md` 加 `llm-foundations/` 的 F00–F07）。前两者是学习索引，只指路不教；八篇讲 LLM 原理 |
| 主线 | `lessons/` | 27 课，每课一个 `README.md`。目录名不带编号，课号由 `mkdocs.yml` 的 nav 位置决定 |
| 原则 | `principles/` | 12 条，一条一个文件 |
| 查阅 | `reference/` | 术语表、技术选型、框架一览、外部资料、诊断题 |

两个导航页不属于任何一课，改课程结构时要同步：`lessons/README.md` 管知识体系（两张地图、每个 Part 的定位和学前自测），
`docs/index.md` 管选路线和接着读。自测题只放在 `lessons/README.md` 各 Part 开头的折叠块里，不单独成页。

可运行的**参考实现**在 [ai-app-engineering-ref](https://github.com/lance2016/ai-app-engineering-ref)：七个里程碑 M0–M6，加 framework-lab 和 capstones。

`docs/` 下都是指向上述目录的符号链接，`mkdocs.yml` 用它们建站，源文件只有一份。

## 2. 目录和站点规则

1. **不新增一级目录。** 新内容只能进 `prerequisites/ lessons/ principles/ reference/`。
2. **课程顺序只写在 nav 里。** 目录名英文 kebab-case、不带编号，也永远不改：URL、书签和读者进度全挂在它上面。
3. **不放可运行代码。** 课文里的代码是示意，写在代码块里，不建 `code/` 和 `tests/`。唯一例外是 `prerequisites/llm-foundations/*/code/`，
   那几个脚本靠「跑一下看数字怎么变」才讲得清，必须纯标准库、`python3 xxx.py` 直接能跑。
4. **不出练习题，不建 Dashboard、周计划、学习记录。** 这是教程，不是考卷，也不是读者的笔记本。
5. **不在课文里推销某个框架。** 不引用已拆走的 `project/`、`tests/`、`prerequisites/python|backend|algorithms`。

加一课：

```bash
mkdir lessons/<slug> && $EDITOR lessons/<slug>/README.md   # 1. 写课
# 2. mkdocs.yml 的 nav 里插一行，位置就是学习顺序，编号随便写
uv run python scripts/sync_numbering.py                    # 3. 课号全站对齐
```

第 3 步自动改首页目录序号、`lessons/README.md` 课程表、每课的 h1 和上下课导航、nav 标签。改不了的会标 `BY HAND`，
通常是首页课程目录里缺的那一行（放哪个 Part 是你的判断）和正文里「NN 课」这类计数。不要手写课号，不用加重定向，不动 `progress.js`。

**首页 `docs/index.md` 是排版页。** 它用 `md_in_html` 写带 class 的 HTML 骨架，链接一律写 Markdown 语法，class 靠 `attr_list` 挂：

```markdown
[<span>01</span> 模型选型与成本](lessons/how-llms-work/README.md){ .lsn data-lesson="how-llms-work" }
```

raw HTML 的 `href` 不会被 MkDocs 改写，也逃过 `check_links.py`，别那样写。`data-lesson` 是进度脚本认课的唯一依据，值必须是 slug。

**图示约定全站共用**，图例在 [课程总览](lessons/README.md)。三个维度各管一件事，别让它们重复表达：
**颜色说这一块归谁管，形状说它是什么，线说它们怎么交互。**

| 语义 | class | 描边色 | 什么时候用 |
|---|---|---|---|
| 模型的概率性决策 | `model` | `#7c6ee6` | LLM、Agent、Router、Planner |
| 运行时与确定性代码 | `runtime` | `#0d806b` | 循环、工具执行、守卫、业务代码；正常终态也用它 |
| 数据、上下文与状态 | `data` | `#4e83a3` | 消息、上下文、事件线程、记忆、检索、存储、trace |
| 需要人 | `human` | `#b88428` | 用户、人工确认、审批、等待人工 |
| 失败与风险 | `risk` | `#b5472d` | 错误、超时、预算耗尽、越权、降级、被拒 |
| 外部或不区分角色 | 不标 | 默认灰 | 第三方系统，或这张图不谈分层 |

形状：`[矩形]` 组件或动作，`{菱形}` 判断，`[(圆柱)]` 持久化存储，`([圆角])` 起点或终态，`((圆形))` 概念。
线：`-->` 同步主流程，`-.->` 异步、可选或离线。

写法是 mermaid 自己的 `classDef`，**只设描边，不设 fill 和 color**——填充和文字跟着主题走，同一份色值在亮暗两套皮肤下都成立：

```text
classDef model stroke:#7c6ee6,stroke-width:2.2px
class D model
```

色值必须和 `wabi.css` 顶部的 `--ws-fig-*` 一致（`classDef` 不接受 `var()`，只能手抄）。不写 `style`，不写 fill。

**只在颜色帮得上忙的图里上色。** 一张只有一条线的示意图、或者通篇同一个角色的图，保持默认灰；
每张图只定义它用到的那几行 `classDef`。`wabi.css` 里没有节点级的 mermaid 规则，
因为 Material 把每张图渲染进 closed shadow root，外部选择器进不去——那里只留全局 CSS 变量。

**Callout 只有两种。** `!!! note "……"` 管来源（见 4.3），`!!! warning "……"` 管不知道会踩坑的事实。别加第三种，
别用 `tip`、`info`，没做样式，会露出 Material 的蓝色卡片。样式规则看 `docs/stylesheets/*.css` 开头的注释。

**例子折起来。** 走一遍具体材料的段落写成 `<details class="case" markdown="1">`，读者点开才看：

```markdown
<details class="case" markdown="1">
<summary>例子：客服机器人在第 48 轮收到 400，聊得短的用户从来没事</summary>

（报文、数字、算式、表格）

</details>
```

三条硬要求。

1. **`<summary>` 里写清那个具体场景**，不许只写「例子」。不点开的读者只看得到这一行，3.3 要的第一屏具体问题全靠它。
2. **例子整个在折叠里**：材料、走一遍的过程、收尾那一句，还有标来源的 `!!! note`（它标的是折叠里那些数字，放外面就变成在说一段看不见的报文）。
3. **折叠块外面的正文不引用折叠里的东西。** 别写「上面那段 JSON」「这条 400」，跳过例子的人接不上。
   同一个结论两边都要说的时候，里面说具体那次（「这条 400 的根在选型那一步」），外面说一般情况（「模型选型要答三个问题」）。

开头那个例子不再单独起一个 h2。折叠标题已经把场景说了，再加一个「第 48 轮，一条 400」这样的标题，
不点开的人只看到一句谜语，点开的人看到同一件事说两遍。

## 3. 一课的结构

这一节是编辑原则，不是文章模板。它规定质量底线和怎么做判断，不规定每课长什么样。

### 3.0 结构由知识决定

写一课之前先回答四个问题：

1. 这一课只解决哪个核心问题？
2. 读者最容易在哪里理解错？
3. 什么材料最容易把它讲清楚？
4. 最自然的理解顺序是什么？

答完再决定二级标题、案例、图、表、代码和 callout。**不许先建一套章节骨架，再往里填内容。**
27 课看起来各不相同，是期望结果。

### 3.1 唯一固定的部分

frontmatter、h1 和底部的上下课导航。课号和导航由 `sync_numbering.py` 回填。

```markdown
---
status: complete
structure: narrative        # 新写和重写的课都加这一行，见 3.7
part: Part N 名称
estimated_time: 约 N 小时
---

# NN 课程标题

> 导语。说这课解决什么问题、为什么值得单独一课。从上一课或读者已有的东西起步。

（正文。小节标题和顺序由主题决定。）

---

[← 上一课 NN](../xxx/README.md) · [下一课 NN →](../xxx/README.md)
```

### 3.2 每课必须让读者拿到的信息

六项是**必须覆盖的信息，不是必须存在的标题**。放在哪个小节、叫什么名字、什么顺序，由主题决定。

| 读者必须拿到 | 说明 |
|---|---|
| 这课解决什么问题 | 导语和开头承担，见 3.3 |
| 学完应该掌握什么 | 要可验证。不必叫「学习目标」，也不必每条以「能……」开头 |
| 至少一种真实失败方式或误解 | 这个机制在生产里具体怎么坏，或者读者最常在哪一步想错 |
| 如何验证这个机制 | 拿什么样本、什么指标、怎么观察。正文里要出现「怎么测」这个说法，脚本靠它查。每课都要留下以后能进 golden set 的东西 |
| 与参考实现的关系 | 一处指向 ai-app-engineering-ref 对应里程碑的链接。这是正文里唯一允许指向代码仓库的地方，只指路，不布置任务 |
| 关键事实的出处 | 外部链接带访问日期（YYYY-MM-DD）；数字、日志、trace 按 4.3 标来源 |

前置课只列真正需要的，附一句为什么。**引用背景知识 F00–F07 时不能假设读者读过**：那一组在 nav 里排在主线之后，
站点写着「都不是必修」。正确写法是说清这一课不解释什么，给出链接，写明不用先读完。正文里点名某一篇 F 也要带链接。

### 3.3 开头：尽快让读者看到一个具体问题

形式由主题决定：一次真实事故、一段异常输出、一组 Request / Response、一份 prompt diff、一段代码、一个反直觉现象、
一个最小实验、一个用户问题，都行。禁止抽象铺垫和空泛的行业背景。「X 会导致 Y，所以必须 Z」是规范文件的语气，不是开场。
也别让 27 课全部变成「事故故事 → 原理」，那只是换了一套模板。讲原理的课，一个数值实验往往比构造一个事故更直接。

**开头压不过「读者得看得懂」。** 把开头几段的名词列出来，逐个问「读者在前面哪一课见过它」。见过的直接用；
没见过而且是这一课要教的，先用最少篇幅说清它是什么，再讲问题。这一小段只讲读懂那个问题所必需的，不是把整课提前讲一遍。

导语是全页第一行，最容易在这里失守。两种情况要分清：**宣告这一课要造什么**，可以用还没解释的名字；
**把一个概念当读者已知的类别来用**，不行。前者是目录，后者是漏讲。

开头那个具体问题按第 2 节的写法折起来：折叠标题给场景，折叠里给完整的例子，折叠外的第一段正文
用一般说法把同一件事再交代一次。已经信这件事的读者一眼跳过整块，还是接得上这一课在讲什么。
导语这时候承担得更多，因为它和折叠标题就是不点开的读者能看到的全部。

### 3.4 材料

**每课至少要有能验证或观察所讲机制的具体材料。** 不规定材料长什么样。全是示意代码的课，读者没法判断作者见过没见过。

- 工程课优先用：日志、trace、HTTP 报文、prompt diff、代码 diff、错误信息。
- 原理课可以用：数值实验、token 序列、概率变化、小型推导、最小输入输出、伪代码。

材料的来源必须标明，见 4.3。

### 3.5 图、表、callout、总结

每一个都要能回答「删了它读者少知道什么」。删不掉才留。

- **图**只在关系、状态、时序或数据流仅靠正文难以理解时才画。图重复了紧邻的正文或表格，就删图。
  流程图画不出来的东西（比如向量空间散点图，点和点的距离本身就是内容）才建 `images/` 放 SVG。
- **表**适合横向比较结构化信息。因果、推导、经验判断和故事优先用正文。一张表不超过六列。
- **总结**不写。一节说完就停，不为收尾总结，不为有力造金句。
- **Callout** 只有第 2 节说的两种。

### 3.6 框架映射是条件项

只有 LangGraph、OpenAI Agents SDK、Claude Agent SDK 三个框架都确实有对应抽象，而且比较它们能帮读者理解机制，
才写一张框架映射表，表后点出「哪一项三个框架都不管」，附官网链接和核对日期。不满足就不写，不为格式完整强行生成。

### 3.7 Legacy：已废弃的固定模板

2026-09-07 以前的部分课程用过一套固定的十二节模板。**已经废弃，新写和重写的课禁止使用**，旧课修改时逐步迁移。
模板全文不再保留在这里，唯一副本是 `check_lesson_template.py` 里的标题清单，只用来盯还没迁移的旧课：

- 写了 `structure: narrative`：脚本不查标题，只查「怎么测」、ai-app-engineering-ref 链接、带访问日期的出处。其余靠评审。
- 没写：脚本仍按旧标题查，防止旧课在小修中掉节。重写一课时就加上这一行。

已迁移的样板：05 Tool Calling、06 Agent Loop、11 MCP、19 可观测性、23 产品设计与 UX。写新课之前先读一两课。

## 4. 文字

- **中文正文，代码和标识符英文。** 代码注释可以中文。
- **示意代码要能看懂，不追求能跑。** 省略 import、日志、错误处理，只留说明机制的行。
  一组示意代码前面说一句「只为说明机制，省略了 X 和 Y，不能直接运行」。**每段代码后面必须有解释。**
  代码块本身怎么标语言、怎么标重点、什么时候用 tabs，见 4.4。
- **加粗只用在真正的重点上**，一节里不超过两三处。
- **不用「我们」「让我们」。** 直接说事。
- **每个外部链接带访问日期。**

### 4.1 说人话

第一标准是**读者一遍就看懂**，不是显得专业。参照 OpenAI 和 Stripe 文档的语气：直接、具体、不铺垫。

- **一句话说一件事。** 超过两个逗号就该考虑断句。
- **先给具体场景，再给结论。** 「用户连问三轮，第三轮模型忘了第一轮说的话」比「上下文管理至关重要」有用。
- **动词要实。** 「网关把请求转给模型」，不写「请求在网关层完成分发」。
- **删掉不影响意思的词。** 强大、优雅、健壮、极致、显著、有效，去掉句子照样成立。

| 别写 | 改成 |
|---|---|
| 对……进行优化 / 处理 | 优化…… / 处理…… |
| 通过 X 实现 Y | 用 X 做 Y |
| 使得……成为可能 | ……就能…… |
| 在……方面 / 对于……来说 | 直接说主语 |
| 值得注意的是 / 总的来说 | 删掉 |
| 不仅……而且…… | 拆成两句 |

**英文习语不直译**：一等公民、咬人、优雅地降级、开箱即用、银弹、显著提升。判断只有一条：**中国程序员当面说话会这么说吗？**
不会就换。互联网黑话同理：赋能、抓手、打法、颗粒度、沉淀、底子。「魔法」「反馈闭环」日常真在用，可以留。

**「不是 X，是 Y」别用滥。** 写之前问一句：X 是读者真会有的误解吗？不是，就直接说 Y，别立稻草人。

另外三种 AI 腔：三连排比（「它简单、它高效、它可靠」）、每节末尾的升华句、正文里的 emoji 和装饰符号。

自查方法：写完念一遍。念着卡壳、或者一口气念不完的句子，就是写坏了。

### 4.2 句式配额

`scripts/check_writing.py` 数正文里的几种句式，超了就退出 1，CI 跑它。三项上限是零：先报数的引导句（「三个要点：」）、
「取舍」条目以加粗句收尾、一个小节里超过三处正文强调。「不是 X，是 Y」和「真正」是棘轮：上限停在改脚本时的实测值，
往下调是常事，往上调要在 commit 里说理由，加一课是理由。

只限量不禁止，因为讲机制的课本来就在纠正「你以为它这样，其实它那样」，全改掉会把声音改没。
真正的毛病是同一个改写反复用。总量卡住之后，想加一个就得先删一个，那时候自然会发现哪几个是一个模子出来的。

### 4.3 数字和记录必须标来源

这门课要给读者看 trace、日志、账单和切片表。看起来像真的，就必须说清是不是真的。只有三条路：

| 来源 | 怎么写 |
|---|---|
| 来自维护者的语音机器人项目 | 点名写清是哪一段经历 |
| 为讲清机制构造的 | 紧挨着放一个 `!!! note`，说明哪些字段是编的 |
| 手上没有材料 | 留 `TODO`，或者不写这个例子 |

**不许出现第四种：看起来像真实数据、没有出处、也没标构造。** 它读起来像诚实的，所以最坏。
**不许编造维护者的经历。** 「我曾经在线上遇到……」只能写真发生过的；没有相关经历就不写这一段。

### 4.4 代码块

课文里的代码只有两种，第 00 课已经跟读者交代过：**示意代码**（说明机制，默认跑不起来）和**可复制的最小例子**
（少数几课才有，正文里明确标注）。下面这些规则服务同一个目的——读者扫到一段代码，应该马上知道它为什么在这里、
哪几行最重要、和上一段是什么关系。

1. **每个代码块都要声明语言。** `python` `jsonc` `json` `yaml` `toml` `sql` `mermaid`；日志、trace 树、
   终端回显、时间线、伪代码一律 `text`。别为了拿高亮把 JSON 标成 `python`。
2. **命令和输出分开。** 给读者复制去跑的命令用 `bash`，不带 `$`；要模拟一整段终端会话（命令和回显混在一起）
   才用 `console`，那时才带 `$`。别在 `bash` 块里塞几十行程序输出。
3. **一个代码块只讲一个想法。** 讲不完就拆两块，中间用一句话说清为什么还要看下一块。
4. **默认不加 `linenums`。** 只有真要逐行走读时才加。目前全课一处都没有，这是对的。
5. **`title=` 只写[参考实现](https://github.com/lance2016/ai-app-engineering-ref)里真实存在的模块路径**，
   例如 `runtime/loop.py`，作用是让读者知道这段示意代码对应仓库里的哪一块。写不出真路径就别写，
   「示例代码」「代码如下」这种标题一律不要。同一节里几块代码都落在同一个文件里也别写——四个一样的标题只是噪音。
6. **`hl_lines` 是「看这里」，不是「危险」。** 只标 1～5 行，而且必须是紧接着的正文马上要讲的那几行。
   整块几乎全标等于没标。
7. **正文提到被标出的行时要能自己站住。** 写「`if not line` 那两行」可以，写「标出来那两行」不行——
   GitHub 上没有高亮。
8. **老师讲课的话用 annotation。** 一句解释「为什么这一行重要」的话写成行尾 `# (1)!`，
   代码块后面紧跟一个有序列表。一块最多 4 条。
9. **`# ←` 留给三五个字的短标签**，说明这一行在做什么，仍然写在行内。分不清就问一句：
   这句话删掉，代码还看得懂吗？看得懂，它就是老师的话，搬进 annotation。
10. **普通注释只写代码自身的语义。** `# 只攒，不解析` 留着，「注意这里非常关键」不是注释。
11. **同一个概念的多种实现用 tabs**，比如三家 API 写同一个请求。**递进关系不能用 tabs**：
    最小版本、加预算、加失败处理是一条路，不是三个选项，读者必须看得到顺序。
12. **递进的几块代码要在小节开头说清是在往上加什么。** 别让六块代码看起来像六个互不相关的例子。
13. **超过 30 行就想想能不能拆。** 长在 import、初始化、异常处理上就删掉；长在逻辑上就按函数边界切成两块。
    实在要给全的，用 `??? example "完整实现"` 折起来。
14. **不为统一而改。** 已经清楚的代码块不需要补标题、行号或 annotation。
15. **MkDocs 的花样不能吃掉 GitHub 的可读性。** tabs、annotation、details 在 GitHub 上都会退化成朴素文本，
    退化之后正文还得读得通。

## 5. 状态、前置和原则

frontmatter 的 `status`：

- `outline`：只有「学完掌握什么」和核心机制的说明
- `draft`：正文基本完整，3.2 的六项还缺一两项
- `complete`：六项都到位，示意代码有解释，链接可达。没迁移的旧课额外要求旧标题齐全

**前置 F 组**：只写结论和纯 Python 小实验，不训练模型，不依赖 torch。目标是被追问「为什么需要 KV cache」时能答出机制。
结构比主线课自由，但要有「为什么需要」和「和主线的关系」。

**原则**：一条一个文件，结构是「原则一句话 → 为什么 → 反例 → 正确形态 → 对照」。不放长代码，最多十行。

## 6. 改完之后

```bash
uv run python scripts/sync_numbering.py            # 课号按 nav 对齐（会改文件）
uv run python scripts/check_links.py               # 相对链接可达，课号标签没写错
uv run python scripts/check_lesson_template.py     # narrative 课查 3.2 的三样信息，旧课查标题
uv run python scripts/check_writing.py             # 句式配额没涨（见 4.2）
uv run mkdocs build --strict                       # 站点能建
```

五条都过才算完，CI 跑的就是这五条（`sync_numbering.py` 在 CI 里加 `--check`，只报不改）。
