# AGENTS.md｜给 AI 协作者的续写说明

这份文件的读者是 AI 编码助手（Claude Code、Codex、Cursor 等）。人类维护者会在新会话里说「改一改第 N 课」或「补一条原则」，你需要靠这份文件独立完成，不用重新问项目是什么。

## 1. 项目是什么

一门中文开源课程，**只讲理论和机制**，不提供可运行的项目代码。

| 层 | 目录 | 形式 |
|---|---|---|
| 背景知识 | `prerequisites/` | 分两类：**工程能力**（`engineering-foundations.md`）和**算法基础**（`algorithm-foundations.md` 加 `llm-foundations/` 的 F00–F07 八篇）。前两者是学习索引，只指路不教；八篇讲 LLM 原理 |
| 主线 | `lessons/` | 26 课，每课一个 `README.md`。目录名不带编号，编号由 `mkdocs.yml` 的 nav 位置决定 |
| 原则 | `principles/` | 12 条，一条一个文件，12-factor-agents 风格 |
| 查阅 | `reference/` | 术语表、技术选型、框架一览、外部资料、诊断题 |

另有两个导航页，不属于任何一课，但改课程结构时必须同步：
`lessons/README.md`（两张地图、能力域清单，以及每个 Part 的定位、学前自测和出师标准）、
`prerequisites/README.md`（背景知识的两类分法，以及那两页学习索引的入口）。

**分工是固定的：** `docs/index.md` 管选路线和接着读，`lessons/README.md` 管知识体系。
自测题不再单独成页，2026-09-06 拆进了 `lessons/README.md` 每个 Part 开头的折叠块，
每组三到五题，作用是「这个 Part 你要不要读」，不算分。要加题就加进对应的 Part，不要再建独立的自测页。

配套的**参考实现**在另一个仓库 [ai-app-engineering-ref](https://github.com/lance2016/ai-app-engineering-ref)：一个完整的 AI 应用服务，七个里程碑 M0–M6，加 framework-lab 和 capstones。本仓库**不再包含任何可运行的项目代码**，2026-09-05 拆分完成。

`docs/` 下都是指向上述目录的符号链接，`mkdocs.yml` 用它们建站，源文件只有一份。

## 2. 硬性目录规则

1. **不新增一级目录。** 新内容只能进 `prerequisites/ lessons/ principles/ reference/`。
2. **课程顺序只写在 `mkdocs.yml` 的 nav 里。** 目录名不带编号（`lessons/tool-calling/`），课号 00–25 由 nav 的位置算出来。要插一课就插在 nav 里该在的位置，不要为了避免重排把它挂在末尾。
3. **目录名英文 kebab-case，标题中文。** 不改已有目录名：目录名就是这一课的永久身份，URL、书签和读者的学习进度全挂在它上面。

### 2.1 怎么加一课

```bash
mkdir lessons/<slug> && $EDITOR lessons/<slug>/README.md   # 1. 写课
# 2. mkdocs.yml 的 nav 里插一行，位置就是学习顺序，编号随便写，脚本会纠
uv run python scripts/sync_numbering.py                    # 3. 编号全站对齐
```

第 3 步会自动改：首页目录的序号、`lessons/README.md` 的课程表、每课底部的上下课导航、每课 h1、nav 标签。
它改不了的会打上 `BY HAND` 让你自己处理，通常是两类：首页课程目录里缺的那一行（放哪个 Part、叫什么名字是你的判断），
以及正文里「26 课」这类计数（可能要改数字，也可能要改说法）。

**不需要做的事：** 改目录名、改链接、加重定向、动 `progress.js`。旧地址由 `mkdocs.yml` 里那批
2026-09-06 留下的 redirect 接着，读者进度按 slug 存，插课不会让任何人的进度失效。
4. **不放可运行的项目代码。** `lessons/` 里的代码是示意用的，写在 README 的代码块里，不建 `code/` 目录，不建 `tests/`。需要落地的东西写进「工程落地」小节，或者指向参考实现仓库。
   唯一的例外是 `prerequisites/llm-foundations/*/code/`：那几个脚本靠「跑一下看数字怎么变」才讲得清（attention 在算什么、温度怎么改变分布、KV cache 有多大），**必须纯标准库、`python3 xxx.py` 直接能跑**，不引入任何依赖。
5. **不创建 Dashboard、周计划、学习记录、复盘之类的文件。** 那是读者自己的事。
6. **README 的课程总表按 Part 分组**，只有 Part 和课号两套编号。不要再引入新的编号体系。

## 2.5 站点外观与图示规范

`docs/` 除了符号链接，还有三个文件决定站点长什么样，改之前先看它们各自的开头注释：

| 文件 | 管什么 |
|---|---|
| `docs/stylesheets/wabi.css` | 阅读皮肤：纸色、墨色、青瓷、细线、无阴影无卡片，正文 36rem 窄栏 |
| `docs/stylesheets/home.css` | 首页版式（56rem 宽栏）、课程总览上那张系统全貌图，加课程页底部那个「标记为已掌握」控件 |
| `docs/javascripts/progress.js` | 学习进度和路径选择，只用 localStorage，没有账号也没有后端 |

**首页 `docs/index.md` 是排版页，不是普通 Markdown。** 它用 `md_in_html` 写成带 class 的 HTML 骨架，
链接一律写 Markdown 语法，class 和属性靠 `attr_list` 挂上去：

```markdown
[<span>01</span> 模型选型与成本](lessons/how-llms-work/README.md){ .lsn data-lesson="how-llms-work" }
```

raw HTML 里的 `href` 既不会被 MkDocs 改写成站点 URL，也逃过 `check_links.py`，所以不要那样写。
`data-lesson` 是进度脚本认课的唯一依据，值必须是这一课的 slug，`<span>` 里是它的课号——两者都由
`sync_numbering.py` 回填，手写错了脚本会纠。进度计数、那把尺规和首屏的「接着读」按钮全靠这份列表，
漏一课那一课就既标不上、也接不上。尺规的长度、「N 课已掌握」里的数字都从列表长度来，加课不用改。

**全站示意图共用一套视觉约定**，画新图时直接套，不要再发明配色：

- **青瓷色**＝正在讲的那条路径；**铁锈色**＝失败、风险、降级。其他一律墨色。
- **实线**＝运行时同步流；**虚线**＝可选调用、异步或离线。
- **矩形**＝组件；**圆形**（`((...))`）＝概念；**菱形**（`{...}`）＝判断。

Mermaid 里只要写 `class NodeName path` 或 `class NodeName risk`，颜色由 `wabi.css` 决定，
不要在图里写 `style` 和十六进制色值。这套约定的图例在[课程总览](lessons/README.md)那张系统全貌图下面。

**Callout 只有两种，而且只干一件事。** 2026-09-07 加的，`wabi.css` 把 Material 那套「浅色卡片
＋图标＋左侧色条」全脱掉了，剩下一条细线加一个 mono 小标签，用的是首页 eyebrow 那套语言。

```markdown
!!! note "这段时间线是构造的教学案例"

    正文，缩进四格。

!!! warning "这两个名字已经废弃"

    会咬人的事实：废弃的字段名、某一家必填另一家可选的参数。
```

`note` 管来源（见 4.3），`warning` 管「不知道会踩坑」的事实。**不要加第三种**，多一种它就变回装饰了。
别用 `!!! tip`、`!!! info` 这些，没做样式，会露出 Material 的蓝色卡片。

## 3. 一课的结构

这一节是编辑原则，不是文章模板。它规定每课的质量底线和写作时怎么做判断，不规定每课长什么样。

### 3.0 结构由知识决定

课程结构由知识本身决定，不由全站模板决定。

写一课之前先回答四个问题：

1. 这一课只解决哪个核心问题？
2. 读者最容易在哪里理解错？
3. 什么材料最容易把它讲清楚？
4. 最自然的理解顺序是什么？

回答完之后再决定二级标题、案例、图、表、代码和 callout。
**禁止先创建统一的章节骨架，再往里面填内容。** 不同课程看起来不一样是正常的，而且是期望结果。

为什么把这条放最前面：2026-09-07 的审查（`anti-slop/audit-001-2026-09-07.md`）发现 26 课二级标题逐字相同、
顺序相同，「取舍」91 条里 46 条标题是「A vs B」，每课读起来像同一篇文章换了名词。那是模板造成的。

### 3.1 唯一固定的部分：frontmatter 和上下课导航

```markdown
---
status: complete
structure: narrative        # 新写和重写的课都加这一行，含义见 3.7
part: Part N 名称
estimated_time: 约 N 小时
---

# NN 课程标题

> 导语。管定位：这课解决什么问题，为什么值得单独一课。从上一课或读者已有的东西起步。

（正文。小节标题和顺序由主题决定。）

---

[← 上一课 NN](../xxx/README.md) · [下一课 NN →](../xxx/README.md)
```

课号、h1 里的编号和底部导航由 `sync_numbering.py` 回填，不要手写。除此之外没有固定结构。

### 3.2 每课必须让读者拿到的信息

下面六项是**必须覆盖的信息，不是必须存在的标题**。可以分散在任何小节里，不要求固定标题，不要求固定顺序。

| 读者必须拿到 | 说明 |
|---|---|
| 这课解决什么问题 | 导语和开头承担，见 3.3 |
| 学完应该掌握什么 | 要可验证。不必叫「学习目标」，也不必每条以「能……」开头：26 课都那么写过，读起来完全一样 |
| 至少一种真实失败方式或误解 | 这个机制在生产里具体怎么坏，或者读者最常在哪一步理解错。全站质量最高的内容都属于这一类 |
| 如何验证这个机制 | 拿什么样本、什么指标、怎么观察。正文里要出现「怎么测」这个说法，`check_lesson_template.py` 靠它查。评测不是第 18 课才开始的事，每课都要留下以后能进 golden set 的东西 |
| 与参考实现的关系 | 一处指向 ai-app-engineering-ref 对应里程碑的链接，映射见那个仓库的 `project/README.md`。**这是正文里唯一允许指向代码仓库的地方**，只指路，不布置任务 |
| 关键事实的出处 | 外部链接带访问日期（YYYY-MM-DD）；数字、日志、trace 按 4.3 标来源 |

前置课只列真正需要的，附一句为什么。**引用背景知识（F00–F07）时不能假设读者读过。** 那一组在 nav 里排在
主线之后，站点自己写着「都不是必修」，所以「前置 F 组讲了 X，本课不再解释」是错的。正确写法是说清这一课
不解释什么，给出链接，写明不用先读完（第 01、04、22 课是样板）。正文里点名某一篇 F 也要带链接，只在前置
小节给链接不够：那一节恰好是已经会的人会跳过的。

### 3.3 开头：尽快让读者看到一个具体问题

开头必须尽快让读者看到一个具体问题，**形式由主题决定**。可以是：

- 一次真实事故
- 一段异常输出
- 一组 Request / Response
- 一份 prompt diff
- 一段代码
- 一个反直觉现象
- 一个最小实验
- 一个用户问题

禁止抽象铺垫和空泛的行业背景。「X 会导致 Y，所以必须 Z」是规范文件的语气，不是开场。
**也不要让 26 课全部变成「事故故事 → 原理」**，那只是换了一套模板。讲原理的课用一个数值实验或一段
反直觉的输出开场，往往比构造一个事故更直接。

**开头压不过「读者得看得懂」。** 判断方法：把开头几段的名词列出来，逐个问「读者在前面哪一课见过它」。
见过的直接用；没见过而且是这一课要教的，先用最少的篇幅说清它是什么，再讲问题。这一小段只讲读懂下面
那个问题所必需的，不是把整课提前讲一遍。2026-09-07 有两课栽在这里：第 05 课开头直接上 `tool_call`
和幂等键，而「工具调用」本身的解释排在第四节；第 11 课直接上 `tools/call` 和 `-32602`，而 `tools/list`
还没讲。两课现在都在前面补了一小节。第 12 课的导语是全站做得最好的一处：它把 Tool、MCP、Skill、
Plugin、A2A 五个词各用一句大白话注释了一遍，然后才开始讲。

导语是全页第一行，同一条检查最容易在这里失守。两种情况要分清：**宣告这一课要造什么**，可以用还没
解释的名字（第 19 课导语里的 OTLP 就是这么用的，它在说「我们会按这个格式发出去」）；**把一个概念当
读者已知的类别来用**，不行。前者是目录，后者是漏讲。

### 3.4 每课都要有能验证或观察这个机制的具体材料

核心要求只有一句：**每课至少要有能够验证或观察所讲机制的具体材料。** 不规定材料长什么样。
全是示意代码的课，读者没法判断作者见过没见过。

- 工程课优先用：日志、trace、HTTP 报文、prompt diff、代码 diff、错误信息。
- 原理课可以用：数值实验、token 序列、概率变化、小型推导、最小输入输出、伪代码。

不强制每课都有日志或报文。一课讲 attention 在算什么，十行数值实验比一份构造的事故日志更有说服力。
材料的来源必须标明，见 4.3。

### 3.5 图、表、callout、总结：删了它读者少知道什么

每一个都要能回答「删了它读者少知道什么」。删不掉才留。

- **图。** 只有关系、状态、时序或数据流仅靠正文难以理解时才画图。如果图只是在重复紧邻正文或表格的
  信息，删除。2026-09-05 清过一轮，24 张配图里 20 张是这种重复；2026-09-07 又删了第 05 课一张把上面的
  sequenceDiagram 重画一遍的 flowchart。要画就按 2.5 的视觉约定走；流程图画不出来的东西才建 `images/`
  放 SVG，比如第 04 课那张向量空间散点图，点和点之间的距离本身就是内容。
- **表。** 表格适合横向比较结构化信息。因果关系、推导过程、经验判断和故事优先用正文。一张表不超过六列。
- **总结。** 一节说完就停。不要为了收尾写总结，不要为了有力造金句，见 4.1 和 4.2。
- **Callout。** 只有 2.5 说的两种，只干那两件事。

### 3.6 框架映射是条件项

不是所有知识都值得比较 LangGraph、OpenAI Agents SDK 和 Claude Agent SDK。只有同时满足两条才写框架映射：

1. 三个框架确实存在对应的抽象；
2. 比较框架能帮读者理解这个机制。

满足了就写一张表，表后点出「哪一项三个框架都不管」，附官网链接和核对日期。
不满足就不写，不要为了课程格式完整强行生成框架表。

### 3.7 Legacy：已废弃的固定模板

2026-09-07 以前的部分课程采用过一套固定的十二节模板，每课从「为什么需要」开头、以「延伸阅读」结尾。
**该模板已经废弃，新写和重写的课禁止继续使用。** 旧课修改时逐步迁移到本节的写法。模板全文不再保留在
这份文件里，剩下的唯一副本是 `check_lesson_template.py` 里那份标题清单，它只用来盯住还没迁移的旧课。

迁移与否由 frontmatter 的 `structure` 字段决定：

- 写了 `structure: narrative`：脚本不查任何标题，只查 3.2 里能用字符串查的三样：「怎么测」、指向
  ai-app-engineering-ref 的链接、带访问日期的出处。其余三项靠评审。
- 没写：脚本仍按旧模板的标题查，作用是防止没迁移的旧课在小修中掉节。**这不是许可。** 重写一课时就加上
  `structure: narrative`。

已经迁移的课是样板，写新课之前先读：05 Tool Calling、06 Agent Loop、11 MCP、19 可观测性、23 产品设计与 UX。

## 4. 写作规范

- **中文正文，代码和标识符英文。** 代码注释可以中文，但变量名、函数名、类型名一律英文。
- **示意代码要能看懂，不追求能跑。** 省略 import、日志、错误处理；保留能说明机制的那些行；关键行加 `# ←` 注释。
  一组示意代码前面要说一句「下面的代码只为说明机制，省略了 X 和 Y，不能直接运行」。
- **每段代码后面必须有解释。** 只贴代码不解释，等于没写。
- **加粗只用在真正的重点上**，一节里不超过两三处。
- **不用「我们」「让我们」。** 直接说事。
- **每个外部链接带访问日期。**
- **表格只用来横向比较结构化信息**，因果、推导、经验判断和故事用正文，见 3.5。一张表不超过六列。

### 4.1 说人话（写完必须自查）

这门课的第一标准是**读者一遍就看懂**，不是显得专业。写之前想清楚要说什么，然后用最直的说法写出来。
参照 OpenAI 官方文档和 Stripe 文档的语气：直接、具体、不铺垫。

四条正面要求：

- **一句话说一件事。** 长句拆开。超过两个逗号就该考虑断句。
- **先给具体场景，再给结论。** 「用户连问三轮，第三轮模型忘了第一轮说的话」比「上下文管理至关重要」有用得多。
- **动词要实。** 说清楚谁做了什么：「网关把请求转给模型」，不是「请求在网关层完成分发」。
- **删掉不影响意思的词就删。** 强大、优雅、健壮、极致、显著、有效，这些形容词去掉句子照样成立。

黑名单，出现即改：

| 别写 | 改成 |
|---|---|
| 对……进行优化 / 处理 | 优化…… / 处理…… |
| 通过 X 实现 Y | 用 X 做 Y，或者直接说 Y 怎么来的 |
| 使得……成为可能 | ……就能…… |
| 在……方面 / 对于……来说 | 直接说主语 |
| 值得注意的是 / 需要指出的是 | 删掉，后面那句本身就是重点 |
| 总的来说 / 综上所述 | 删掉 |
| 不仅……而且…… | 拆成两句 |

**英文习语不要直译。** 这一类最难自查，因为它读起来像技术黑话，不像翻译腔。判断方法只有一个：
**中国程序员当面说话时会这么说吗？** 不会，就换掉。

| 别写 | 出处 | 改成 |
|---|---|---|
| 一等公民 | first-class citizen | 内置、框架自带、为它做了建模 |
| 最容易咬人 | bite you | 最容易写错、最容易踩 |
| 既昂贵又不可靠 | both … and | 又贵又不可靠 |
| 优雅地降级 | gracefully | 平稳降级，或者直接说降成什么样 |
| 开箱即用 | out of the box | 装上就能用 |
| 银弹 | silver bullet | 万能解法 |
| 表现良好 | performs well | 跑得好好的 |
| 显著提升 / 显著降低 | significantly | 明显、大幅，或者直接给数字 |

「魔法」是例外，中文程序员日常真在用（「这段代码有魔法」），可以留。
互联网黑话同样不要：赋能、抓手、打法、颗粒度、沉淀。「反馈闭环」是产品圈的通用说法，可以留。

**「底子」也不要。** 2026-09-06 全站清掉了这个词（原来有十三处，「编程与后端底子」「算法与数学底子」
「底子够了从哪开始」）。它不是英文直译，但同样过不了那个测试：当面说话不会这么说。
要表达 foundations，就把话说完整——「这门课会用到什么」「缺了去哪学」。

**「不是 X，是 Y」别用滥。** 这个句式有力，但全课已经用了四十多次。写之前先问一句：
X 是读者真会有的误解吗？不是，就直接说 Y，别立一个稻草人。

还有三种形式上的 AI 腔，一样不要：

- **三连排比**：「它简单、它高效、它可靠」。
- **每节末尾的升华句**。一节说完就停，不要再总结一遍。
- **正文里的 emoji 和装饰性符号**。表格和列表已经够用了。

自查方法：写完念一遍。念着卡壳、或者一口气念不完的句子，就是写坏了。

### 4.2 句式配额（`scripts/check_writing.py` 会数）

4.1 那张黑名单靠自查，一年下来没人数过，于是 26 课长成了同一个样子。现在有脚本数了：

```bash
uv run python scripts/check_writing.py            # 超了就退出 1，CI 跑这个
uv run python scripts/check_writing.py --report    # 只看数字，不判定
```

它数的是**正文**，代码块里的不算。五个上限里三个是零：

| 项 | 上限 | 为什么 |
|---|---|---|
| 「三个要点：」这类先报数的引导句 | 0 | 后面那几段本来就自成一列，这一行只是在宣布 |
| 「取舍」条目以加粗句收尾 | 0 | 加粗把一句普通结论变成金句，见 3.5 |
| 一个最小小节里超过三处正文强调 | 0 | 到处是重点等于没有重点。行首的加粗是列表标签，不算；表格里标反常格，不算 |
| 「不是 X，是 Y」加「而不是」 | 93 | 这个句式本身没错，多数用得对。它坏在重复，见下 |
| 「真正」 | 29 | 同上 |

**后两个是棘轮，不是目标。** 数字就停在改这个文件时的实测值，所以日常写作抬不动它。
往下调是常事：改完一段正文，顺手把数字降下来。往上调要在 commit 里说理由，加一课是理由。

那两个句式为什么只限量不禁止：`AGENTS.md` 4.1 已经给了对的判断标准（X 是读者真会有的误解吗），
而按那个标准逐条看，绝大多数是合格的——讲机制的课本来就在纠正「你以为它这样，其实它那样」。
全部改掉会把声音改没，那是这道滤网另一侧的失败。真正的毛病是**同一个改写反复用**：
「不是 bug，是 X」曾经在三课加一篇前置里出现四次。脚本数不出这种重复，但总量卡住之后，
你想再加一个就得先删一个，那时候自然会发现哪几个是一个模子里出来的。

### 4.3 数字和记录必须标来源

这门课要给读者看 trace、日志、账单和切片表。这类东西一旦看起来像真的，就必须说清它是不是真的。
三条路，只能走这三条：

| 来源 | 怎么写 |
|---|---|
| 来自那个语音机器人项目 | 像「一线经验」那样点名，写清是哪一段经历 |
| 为了讲清机制构造的 | 紧挨着放一个 `!!! note`，说明哪些字段是编的 |
| 手上没有材料 | 留 `TODO`，或者根本不写这个例子 |

**不许出现第四种：看起来像真实数据、没有出处、也没标构造。** 这是最坏的一种，因为它读起来像诚实的。
现在全站还有两处待处理，第 23 课的反馈切片表和第 18 课那句「一个真实的对比」，
见 `anti-slop/audit-001-2026-09-07.md` 的第 9 条。

**不许编造维护者的经历。** 「我曾经在线上遇到……」只能写真发生过的。第 05 课的开头就是这么处理的：
那张退款时间线是构造的，正文里明说了项目里没有资金类工具的事故可讲。

## 5. 状态定义

frontmatter 的 `status`：

- `outline`：只有「学完掌握什么」和核心机制的说明
- `draft`：正文基本完整，但 3.2 的六项信息还缺一两项（多半是失败方式、怎么测或出处）
- `complete`：3.2 的六项信息都到位，示意代码有解释，链接可达。还没迁移的旧课额外要求旧模板的标题齐全，脚本会查

## 6. 写前置模块和原则的差异

**前置 F 组**：只写结论和纯 Python 小实验，不训练模型，不依赖 torch。目标是被追问「为什么需要 KV cache」时能答出机制，而不是背名词。结构比主线课自由，但也要有「为什么需要」和「和主线的关系」。

**原则**：一条一个文件，结构是「原则一句话 → 为什么 → 反例 → 正确形态 → 对照（相关课程和外部出处）」。原则文件不放长代码，最多十行。

## 7. 不要做的事

- 不要写「本课将介绍……」这类无信息的开场
- **不要先建章节骨架再填内容，也不要套 3.7 说的那套旧模板。** 结构由知识决定，见 3.0。现在已经有一大批长得一模一样了
- 不要为了配图画 Mermaid、为了收尾写总结、为了有力造金句，见 3.5
- 不要写没有出处的数字和 trace，也不要编造维护者的经历，见 4.3
- 不要在课文里推销某个框架
- 不要把示意代码写成能跑的完整程序（那是参考实现仓库的事）
- 不要新增 `code/`、`tests/`、`scripts/` 里的运行时代码
- 不要手写课程编号。要改顺序就改 nav，然后跑 `sync_numbering.py`
- 不要给课程目录名加回数字前缀
- 不要引用已经拆走的 `project/`、`tests/`、`prerequisites/python|backend|algorithms`
- **不要出练习题。** 2026-09-06 删掉了全部 26 个 `exercises.md`，共 130 题：这是教程不是考卷，而且拆分之后正文只剩示意代码，「改一改、验收标准是 X」已经无从做起

## 8. 改完之后

```bash
uv run python scripts/sync_numbering.py            # 课号按 nav 对齐（会改文件）
uv run python scripts/check_links.py               # 相对链接可达，课号标签没写错
uv run python scripts/check_lesson_template.py     # complete 单元：narrative 课查 3.2 的三样信息，旧课查标题
uv run python scripts/check_writing.py             # 句式配额没涨（见 4.2）
uv run mkdocs build --strict                       # 站点能建
```

CI 跑的是 `sync_numbering.py --check`：只报不改，有任何一处对不上就红。

五条都过才算完，CI 跑的就是这五条。`check_writing.py` 那条是棘轮，超了的话
要么改正文、要么在同一个 commit 里把上限调下来并说清理由。
