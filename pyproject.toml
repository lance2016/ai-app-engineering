[project]
name = "ai-app-engineering"
version = "0.1.0"
description = "A Chinese course on AI application engineering: LLMs, Agents, RAG, MCP, evaluation, observability and production concerns"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
docs = [
    "jieba>=0.42.1",
    "mkdocs-glightbox>=0.5.2",
    "mkdocs-material>=9.5",
]
dev = [
    "pyyaml>=6.0.3",
]
